# frozen_string_literal: true

RSpec.describe Getting::Start::Demo2 do
  it "has a version number" do
    expect(Getting::Start::Demo2::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(true).to eq(true)
  end
end
